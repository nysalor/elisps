(autoload 'irchat "irchat" "Internet Relay Chat." t)
(setq irchat-nickname "nysalor")
(setq irchat-server "irc.kyoto.wide.ad.jp")
(setq irchat-service 6667)
(setq to-ascii-process ?B)
(setq to-kanji-process ?B)

;; join channel:C-c j
