(autoload 'perl-mode "cperl-mode" "alternate mode for editing Perl programs" t)
;(defalias 'perl-mode 'cperl-mode);(add-hook 'cperl-mode-hook
;          (lambda ()
;            (require 'perlplus)
;            (define-key perl-mode-map "C-c C-c" 'perlplus-complete-symbol)
;            (perlplus-setup)))
